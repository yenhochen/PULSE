# PULSE linear-probe — HAR (6-channel human activity recognition)
base_config: configs/base/pulse_base.yaml

data_args:
  path: data/har/processed
  mode: fullts
  train_stride: 32

training_args:
  batch_size: 128
  eval_batch_size: 128
  save_every_n: 100
