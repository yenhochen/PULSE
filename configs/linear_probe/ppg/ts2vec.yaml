base_config: configs/base/base.yaml
model_type: ts2vec

data_args:
  path: data/ppg/processed
  mode: fullts
  input_dims: 1
  subseq_size: 960
  downsample_factor: 4

model_args:
  temporal_unit: 0

training_args:
  batch_size: 64
  eval_batch_size: 64
  save_every_n: 100
