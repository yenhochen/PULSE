base_config: configs/base/base.yaml
model_type: simclr

data_args:
  path: data/ppg/processed
  mode: fullts
  input_dims: 1
  subseq_size: 960
  downsample_factor: 4

model_args:
  tau: 1

training_args:
  epochs: 100
  batch_size: 64
  eval_batch_size: 64
  lr: 0.001
  save_every_n: 100
